- name: Disable CodeAnalysis for ALL vcxproj
  shell: pwsh
  run: |
    Get-ChildItem -Recurse -Filter *.vcxproj | ForEach-Object {
      $file = $_.FullName
      $xml = Get-Content $file -Raw

      # 关闭 CodeAnalysis / PREfast
      if ($xml -notmatch 'DisableCodeAnalysis') {
        $xml = $xml -replace '</PropertyGroup>', @"
  <DisableCodeAnalysis>true</DisableCodeAnalysis>
  <EnablePREfast>false</EnablePREfast>
</PropertyGroup>
"@
      }

      Set-Content -Path $file -Value $xml -Encoding UTF8
      Write-Host "✔ 已禁用 CodeAnalysis：$file"
    }
